<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submit Recipe</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="w-screen h-screen bg-gray-100 dark:bg-gray-900 flex items-center justify-center">
    <div class="w-full max-w-lg p-8 bg-white dark:bg-gray-800 rounded-lg shadow-lg">
        <!-- Navigation Buttons -->
        <div class="flex justify-between mb-6">
            <a href="/admin/logout" class="text-blue-600 dark:text-blue-400 font-semibold hover:underline">Logout</a>
            <a href="/admin/recipe-list" class="text-blue-600 dark:text-blue-400 font-semibold hover:underline">Recipe List</a>
        </div>

        <h3 class="text-2xl font-semibold text-center mb-6 text-gray-900 dark:text-white">Submit Your Recipe</h3>
        <form action="/admin/create" method="post" class="space-y-4">
            <!-- Recipe Name -->
            <div>
                <label for="name" class="block text-gray-700 dark:text-gray-300 font-semibold mb-2">Recipe Name</label>
                <input 
                    id="name" 
                    name="name" 
                    type="text" 
                    placeholder="Enter recipe name" 
                    class="w-full px-4 py-3 bg-gray-100 dark:bg-gray-700 text-gray-900 dark:text-white rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" 
                    required>
            </div>
            <!-- Recipe Ingredients -->
            <div>
                <label for="ingredients" class="block text-gray-700 dark:text-gray-300 font-semibold mb-2">Ingredients</label>
                <textarea 
                    id="ingredients" 
                    name="ingredients" 
                    rows="4" 
                    placeholder="List ingredients, separated by commas" 
                    class="w-full px-4 py-3 bg-gray-100 dark:bg-gray-700 text-gray-900 dark:text-white rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" 
                    required></textarea>
            </div>
            <!-- Recipe Steps -->
            <div>
                <label for="steps" class="block text-gray-700 dark:text-gray-300 font-semibold mb-2">Steps</label>
                <textarea 
                    id="steps" 
                    name="steps" 
                    rows="6" 
                    placeholder="Describe each step" 
                    class="w-full px-4 py-3 bg-gray-100 dark:bg-gray-700 text-gray-900 dark:text-white rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" 
                    required></textarea>
            </div>
            <!-- Submit Button -->
            <div class="text-center">
                <button 
                    type="submit" 
                    class="bg-blue-600 text-white font-semibold py-3 px-6 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
                    Submit Recipe
                </button>
            </div>
        </form>
    </div>
</body>
</html>
